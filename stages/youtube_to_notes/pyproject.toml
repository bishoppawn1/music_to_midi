[project]
name = "youtube-to-notes-stage"
version = "0.1.0"
description = "Download or ingest audio and transcribe polyphonic note events to JSON"
requires-python = ">=3.10,<3.11"
dependencies = [
  "basic-pitch>=0.4.0,<0.5",
  "setuptools<81",
  "yt-dlp>=2025.1.0",
]

[tool.uv]
package = false
